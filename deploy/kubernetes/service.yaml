apiVersion: v1
kind: Service
metadata:
  name: sovereignloop
  labels:
    app.kubernetes.io/name: sovereignloop
spec:
  selector:
    app.kubernetes.io/name: sovereignloop
  ports:
    - { name: http, port: 8787, targetPort: http }
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: sovereignloop
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: sovereignloop
